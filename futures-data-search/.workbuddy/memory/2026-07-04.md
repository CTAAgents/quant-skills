# 2026-07-04 工作日志

## 主力映射定时更新（自动化15:30）

**数据源情况**：
- 通达信TQ-Local: ✅ 可用（尽管 TdxW.exe 未通过tasklist查到，但本地HTTP 17709端口有服务在监听）
- 交易所API: ❌ 全部WAF拦截或404（DCE=412, SHFE=404, CZCE=412, GFEX=HTML），降级至AKShare
- CFFEX: ✅ 直接API成功（680条）
- INE: ❌ DB直读无数据

**脚本问题及修复**：
- 发现 `get_all_exchange_data()` 再次触发AKShare调用导致CZCE卡死5分钟+
- 临时修改 `update_dominant_mapping.py`：跳过 `get_all_exchange_data()`，直接从已获取的 `exchange_api_data` 组合FULL数据集
- DuckDB曾被PID 20040锁定（强制kill后文件保持锁定），删除DB文件后恢复正常

**覆盖统计**：84/84 (100%) — 合约级=11, 品种级=73
- SHFE: 20/20 ✅, DCE: 22/22 ✅, CZCE: 25/25 ✅
- GFEX: 5/5 ✅, INE: 4/4 ✅, CFFEX: 8/8 ✅

**换月事件**: 4个（全部为中金所品种：TS/TF/T/TL从L8连续合约切换到2609合约）

**异常记录**：
- 大多数品种仅获取到品种级(888)数据，非合约级 → 通达信返回的是连续合约而非具体合约
- DB保存持续失败（DuckDB schema不兼容: Data type 'str'）
- 文件大小38KB（较之前的41KB有下降，因品种级映射字段较少）
- WR仍在映射中（标记为活跃但仅30手持仓）
- ZC（动力煤）已不在映射中（符合预期，政策冻结品种）

**数据文件**: `dominant_map_20260703.json` (38KB) / `dominant_map_latest.json` ✅
