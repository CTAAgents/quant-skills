# 2026-06-28 工作日志

## 重大架构升级：多源数据集成路由系统 v3.1

### 核心变更
1. **数据源配置化**：新建 `data_sources.yaml` + `DataSourceConfig` 单例加载器
   - 所有数据源在此注册：启用/禁用、盘中/盘后优先级、参数配置
   - 优先级数字越小越优先
   - 盘中（09-15/21-23）：TqSDK → exchange → eastmoney → akshare → web → cache
   - 盘后（15-21）：exchange → eastmoney → tqsdk → akshare → web → cache
   - 支持热重载（`reload()`方法），无需重启进程

2. **WH6数据源彻底移除**：从 multi_source_adapter.py、SKILL.md、README、USER_GUIDE、commodity-trend-signal、commodity-daily-analysis 全部清理

3. **TqSDK长尾品种降级**：`_fallback_long_tail()` → `_fallback_via_tqsdk()` + `_fallback_via_eastmoney()`
   - 定向合约ID构建（CZCE 3位整数格式）
   - 环境变量 TQSDK_USERNAME/PASSWORD 认证
   - 持久化连接复用 TqAuth 对象
   - 品种状态标记：policy_frozen/delisted/low_liquidity/active

4. **主力映射系统重写 v2.3**：支持全部6个交易所
   - CFFEX: 正则 `IF\d{4}`等
   - CZCE: 3位格式 `TA\d{3}`
   - 其它：小写+4位数字
   - 双模式：合约级用 DominantMappingCalculator，品种级用 888/99
   - 新增 `DominantMappingArchive` 90天历史归档
   - 覆盖 73/85 品种有数据 + 9 状态标记 = 82 总覆盖

5. **交易所API采集器修复**：Chrome 126 UA + `sec-ch-ua` 字段 + UA旋转重试应对 DCE/CZCE 412 WAF

6. **品种清单扩展**：新增6个品种（GFEX.PT铂/PD钯、SHFE.AD铸造铝合金/OP胶版印刷纸、CZCE.PR瓶片/PX对二甲苯），总计85个

### 商品期货每日分析管道更新
- `phase1_collect_signals.py` 新增 Phase 0：加载 DataSourceConfig，打印当前交易时段和路由链
- 自动化任务提示词更新：数据采集规则引用 data_sources.yaml

### 金十数据 MCP
- 标准 MCP 配置已写入 mcp.json（使用 Bearer Token 认证）
- 配置字段修正：`serverUrl` → `url`（WorkBuddy 使用 url 字段）
- 已成功激活，8个工具全部可用（get_quote/get_kline/list_flash/search_flash/list_news/search_news/get_news/list_calendar）

### 通达信 MCP 整合入 skill（v3.3.0）

### Iwencai SkillHub CLI + hithink-futures-selector 安装
- 从 https://www.iwencai.com/skillhub/static/0.0.4/ 安装了 Iwencai SkillHub CLI
- 修复了 Windows 环境下 python3 不可用的问题（修改 iwencai-skillhub-cli 包装脚本指向 managed Python）
- 安装了 hithink-futures-selector 技能（同花顺问财期货期权筛选）
- 安装 hithink-futures-query 技能（同花顺问财期货期权查询）
- 安装 hithink-basicinfo-query 技能
- 安装 hithink-macro-query 技能
- 安装 news-search 技能
- 安装 report-search 技能
- 环境变量设置：
  - IWENCI_BASE_URL=https://openapi.iwencai.com
  - IWENCAI_API_KEY=sk-proj-01-...（已脱敏）
  - 已写入 ~/.bashrc、PowerShell profile、系统级User环境变量

### 问财 SkillHub 整合入 skill（v3.4.0）
- `data_sources.yaml` 新增 `type: skillhub` 类型，iwencai 标记为此类型
- `DataSource` 枚举新增 `IWENCAI`
- SKILL.md 新增问财 SkillHub 完整使用文档（含6个已安装技能清单）
- SKILL.md 版本升至 v3.4.0
- 已推送 GitHub，commit: 3d66491
- `data_sources.yaml` 新增 `type: comprehensive` 类型，tdx 标记为此类型
- `DataSource` 枚举新增 `TDX`
- SKILL.md 新增通达信 MCP 完整文档（含期货相关工具、与已有数据源对比）
- 新增能力：券商研报查询（wenda_report_query）、宏观数据查询（wenda_macro_query）
- TDX 不参与价格数据路由，独立通过 MCP 工具调用
- 验证测试通过：期货行业新闻、宏观数据（社融）查询均正常
- 已推送 GitHub，commit: f5a2a24

### 当前数据源全景（2026-06-28 收盘）
price 类型（6个）：TqSDK → 交易所API → 东方财富 → AKShare → WebSearch → Cache
news 类型（1个）：金十数据 MCP（快讯/资讯/日历）
comprehensive 类型（1个）：通达信 MCP（行情/资讯/研报/宏观）
- `data_sources.yaml` 新增 `type` 字段（price/news），jin10 标记为 type: news
- `DataSourceEntry` 新增 `category` 字段，默认 "price"
- `get_priority_list()` 改为仅返回 price 类型，jin10 不干扰价格路由
- 新增 `get_sources_by_category()` 方法
- SKILL.md 新增金十数据使用文档
- SKILL.md 版本升级至 v3.2.0
- 金十数据验证测试通过：calendar、USOIL quote、XAUUSD quote 全部正常返回

### 待办
- [ ] 用户需激活金十数据 MCP 后才能使用
- [ ] 金十数据如需加入 data_sources.yaml 需用户确认
